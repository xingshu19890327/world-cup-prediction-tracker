export default function InfoPanel(){ return <section id="info" className="rounded-2xl border border-slate-700 bg-slate-950/70 p-5 text-sm leading-7 text-slate-300"><h2 className="mb-2 text-xl font-bold text-cyan-100">说明&来源</h2><p>赔率不是实时同步。只有点击更新按钮时才尝试抓取一次，成功后保存为静态记录。</p><p>前端调用 /api/sportsbet?url=...，由 Vercel Serverless Function 代理请求 Sportsbet 页面，避免浏览器直接跨站读取时常见的 CORS 限制。</p><p>如果抓取失败，会保留现有赔率，可手动填写或通过 JSON/CSV 导入；本工具仅用于预测记录和复盘，不提供投注建议。</p></section> }
