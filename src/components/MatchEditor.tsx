import type { MatchPrediction } from '../types'; import { groups, rounds } from '../types';
export default function MatchEditor({match,onChange,onClose,onSave}:{match:MatchPrediction|null;onChange:(m:MatchPrediction)=>void;onClose:()=>void;onSave:()=>void}){ if(!match) return null; const input=(k:keyof MatchPrediction,type='text')=><input className="excel-input" type={type} value={String(match[k]??'')} onChange={e=>onChange({...match,[k]: type==='number'?(e.target.value===''?'':Number(e.target.value)):e.target.value})}/>; return <div className="fixed inset-0 z-50 flex items-center justify-center modal-backdrop p-4"><div className="max-h-[90vh] w-full max-w-4xl overflow-auto rounded-2xl bg-slate-900 p-6 shadow-2xl"><div className="mb-4 flex justify-between"><h2 className="text-xl font-bold">编辑比赛 #{match.matchNo}</h2><button onClick={onClose}>✕</button></div><div className="grid gap-3 md:grid-cols-3">{(['matchNo','australiaTime','homeTeam','awayTeam','city','correspondingSbWdlOdds','predictedScore1','predictedScore1Odds','predictedScore2','predictedScore2Odds','predictedScore3','predictedScore3Odds','sbHomeOdds','sbDrawOdds','sbAwayOdds','actualScore','notes','oddsSource'] as (keyof MatchPrediction)[]).map(k=><label key={k} className="text-sm text-slate-300"><span>{k}</span>{input(k, typeof match[k]==='number'?'number':'text')}</label>)}<label>组<select className="excel-input" value={match.group} onChange={e=>onChange({...match,group:e.target.value})}>{groups.map(g=><option key={g}>{g}</option>)}</select></label><label>轮次<select className="excel-input" value={match.round} onChange={e=>onChange({...match,round:e.target.value})}>{rounds.map(r=><option key={r}>{r}</option>)}</select></label><label>胜平负预测<select className="excel-input" value={match.wdlPrediction} onChange={e=>onChange({...match,wdlPrediction:e.target.value as MatchPrediction['wdlPrediction']})}>{['主胜','平局','客胜','TBD',''].map(r=><option key={r}>{r}</option>)}</select></label></div><div className="mt-5 flex gap-2"><button className="rounded-lg bg-cyan-500 px-4 py-2 font-bold text-slate-950" onClick={onSave}>保存编辑</button><button className="rounded-lg bg-slate-700 px-4 py-2" onClick={onClose}>取消</button></div></div></div>}
