import type { FiltersState, QuickFilter, SortKey } from '../types'; import { groups, rounds } from '../types';
export default function Filters({filters,setFilters}:{filters:FiltersState;setFilters:(f:FiltersState)=>void}){ const quick:QuickFilter[]=['全部','已完赛','未赛','ChatGPT赛果命中','Claude赛果命中','ChatGPT比分命中','Claude比分命中','双方都错','双方预测分歧','赔率缺失','高赔率']; const sorts:SortKey[]=['matchNo','australiaTime','round','completionStatus','chatgptWdlHit','claudeWdlHit','predictionDisagreement','highOddsTag','oddsMissing']; const patch=(p:Partial<FiltersState>)=>setFilters({...filters,...p}); return <section className="rounded-2xl bg-slate-950/70 p-4 ring-1 ring-slate-700"><div className="mb-3 flex flex-wrap gap-2">{quick.map(q=><button key={q} onClick={()=>patch({quick:q})} className={`rounded-full px-3 py-1 text-xs ${filters.quick===q?'bg-cyan-400 text-slate-950':'bg-slate-800 text-slate-200'}`}>{q}</button>)}</div><div className="grid gap-2 md:grid-cols-6"><select className="excel-input" value={filters.round} onChange={e=>patch({round:e.target.value})}>{['全部',...rounds].map(x=><option key={x}>{x}</option>)}</select><select className="excel-input" value={filters.group} onChange={e=>patch({group:e.target.value})}>{['全部',...groups].map(x=><option key={x}>{x}</option>)}</select><input className="excel-input" placeholder="球队搜索" value={filters.team} onChange={e=>patch({team:e.target.value})}/><input className="excel-input" placeholder="城市搜索" value={filters.city} onChange={e=>patch({city:e.target.value})}/><select className="excel-input" value={filters.completion} onChange={e=>patch({completion:e.target.value as FiltersState['completion']})}>{['全部','已完成','待赛'].map(x=><option key={x}>{x}</option>)}</select><select className="excel-input" value={filters.sort} onChange={e=>patch({sort:e.target.value as SortKey})}>{sorts.map(x=><option key={x} value={x}>排序：{x}</option>)}</select></div></section> }
