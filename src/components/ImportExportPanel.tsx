import type { MatchPrediction } from '../types'; import { downloadText, matchesToCsv } from '../utils/csv';
export default function ImportExportPanel({matches,onImport,onSave,onRecalc,onReset,onAdd}:{matches:MatchPrediction[];onImport:(s:string)=>void;onSave:()=>void;onRecalc:()=>void;onReset:()=>void;onAdd:()=>void}){ const btn='rounded-lg px-4 py-2 text-sm font-semibold shadow bg-cyan-500 text-slate-950 hover:bg-cyan-300'; return <div className="flex flex-wrap gap-2"><button className={btn} onClick={onAdd}>新增比赛</button><button className={btn} onClick={onSave}>保存</button><button className={btn} onClick={onRecalc}>更新结果 / 重新计算</button><label className={btn}>导入 JSON<input type="file" accept="application/json" className="hidden" onChange={e=>{const f=e.target.files?.[0]; if(f) f.text().then(onImport)}}/></label><button className={btn} onClick={()=>downloadText('world-cup-tracker.json',JSON.stringify(matches,null,2))}>导出 JSON</button><button className={btn} onClick={()=>downloadText('world-cup-tracker.csv',matchesToCsv(matches),'text/csv;charset=utf-8')}>导出 CSV</button><button className="rounded-lg bg-rose-600 px-4 py-2 text-sm font-semibold" onClick={onReset}>重置示例数据</button></div>}
