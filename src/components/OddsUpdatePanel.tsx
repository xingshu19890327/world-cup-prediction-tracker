import type { MatchPrediction } from '../types';
export default function OddsUpdatePanel({matches,onUpdate}:{matches:MatchPrediction[];onUpdate:()=>void}){ const counts=matches.reduce((a,m)=>{const s=m.oddsStatus||(!m.oddsSource?'缺少赔率来源':([m.sbHomeOdds,m.sbDrawOdds,m.sbAwayOdds].some(Boolean)?'已更新':'部分赔率缺失')); a[s]=(a[s]||0)+1; return a;},{} as Record<string,number>); return <section id="odds" className="rounded-2xl bg-slate-950/75 p-5 ring-1 ring-amber-300/20"><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-bold text-amber-100">Sportsbet 赔率更新</h2><p className="mt-2 text-sm text-slate-300">赔率不是实时变化。只有点击“更新 Sportsbet 赔率”时才尝试抓取一次，成功后保存为静态记录。</p><p className="mt-1 text-sm text-slate-400">抓取失败会保留原赔率，用户仍可手动填写或通过 JSON/CSV 导入。</p></div><button onClick={onUpdate} className="rounded-xl bg-amber-400 px-5 py-3 font-black text-slate-950 shadow-lg shadow-amber-950/40">更新 Sportsbet 赔率</button></div><div className="mt-4 flex flex-wrap gap-2 text-xs">{['已更新','缺少赔率来源','抓取失败','部分赔率缺失'].map(s=><span key={s} className="rounded-full bg-slate-800 px-3 py-1 text-slate-200">{s}: {counts[s]||0}</span>)}</div></section>}
