import type { MatchPrediction } from '../types';
const columns: [keyof MatchPrediction,string][] = [['matchNo','场次'],['group','组'],['round','轮次'],['easternTime','美东时间(ET)'],['australiaTime','澳洲时间(AEST)'],['homeTeam','主队'],['awayTeam','客队'],['city','城市'],['wdlPrediction','胜平负预测'],['correspondingSbWdlOdds','对应SB胜平负赔率'],['predictedScore1','比分预测1'],['predictedScore1Odds','比分1赔率(SB)'],['predictedScore1Result','比分1赛果'],['predictedScore2','比分预测2'],['predictedScore2Odds','比分2赔率(SB)'],['predictedScore2Result','比分2赛果'],['predictedScore3','比分预测3'],['predictedScore3Odds','比分3赔率(SB)'],['predictedScore3Result','比分3赛果'],['sbHomeOdds','SB主胜'],['sbDrawOdds','SB平局'],['sbAwayOdds','SB客胜'],['actualScore','实际比分'],['actualResult','实际胜负'],['scoreHit','比分命中?'],['wdlHit','赛果命中?'],['notes','备注'],['oddsSource','赔率来源']];
const esc=(v: unknown)=>`"${String(v ?? '').replace(/"/g,'""')}"`;
export function matchesToCsv(matches: MatchPrediction[]): string { return [columns.map(([,h])=>esc(h)).join(','), ...matches.map(m=>columns.map(([k])=>esc(m[k])).join(','))].join('\n'); }
export function downloadText(filename: string, content: string, type='application/json;charset=utf-8'): void { const blob=new Blob([content],{type}); const url=URL.createObjectURL(blob); const a=document.createElement('a'); a.href=url; a.download=filename; a.click(); URL.revokeObjectURL(url); }
