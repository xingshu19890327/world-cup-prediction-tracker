import { useMemo, useState } from 'react';
import DashboardCards from './components/DashboardCards'; import Filters from './components/Filters'; import ImportExportPanel from './components/ImportExportPanel'; import InfoPanel from './components/InfoPanel'; import MatchEditor from './components/MatchEditor'; import MatchTable from './components/MatchTable';
import type { FiltersState, MatchPrediction } from './types';
import { importMatches, loadMatches, resetMatches, saveMatches } from './utils/storage';
import { recalculateMatch } from './utils/score';
const emptyMatch=(next:number):MatchPrediction=>recalculateMatch({id:crypto.randomUUID(),matchNo:next,group:'',round:'第1轮',easternTime:'',australiaTime:'',homeTeam:'',awayTeam:'',city:'',wdlPrediction:'TBD',correspondingSbWdlOdds:'',predictedScore1:'',predictedScore1Odds:'',predictedScore2:'',predictedScore2Odds:'',predictedScore3:'',predictedScore3Odds:'',sbHomeOdds:'',sbDrawOdds:'',sbAwayOdds:'',actualScore:'',notes:'',oddsSource:''});
export default function App(){ const [matches,setMatches]=useState<MatchPrediction[]>(loadMatches); const [filters,setFilters]=useState<FiltersState>({round:'全部',group:'全部',team:'',city:'',completion:'全部',hit:'全部',sort:'matchNo'}); const [editing,setEditing]=useState<MatchPrediction|null>(null); const recalcAll=(rows=matches)=>rows.map(recalculateMatch); const visible=useMemo(()=>{let rows=[...matches]; rows=rows.filter(m=>(filters.round==='全部'||m.round===filters.round)&&(filters.group==='全部'||m.group===filters.group)&&(m.homeTeam.includes(filters.team)||m.awayTeam.includes(filters.team))&&m.city.includes(filters.city)&&(filters.completion==='全部'||(filters.completion==='已完成'?!!m.actualResult:!m.actualResult))&&(filters.hit==='全部'||(filters.hit==='比分命中'?m.scoreHit==='✅':filters.hit==='比分未中'?m.scoreHit==='❌':filters.hit==='赛果命中'?m.wdlHit==='✅':m.wdlHit==='❌'))); rows.sort((a,b)=>String(a[filters.sort]).localeCompare(String(b[filters.sort]),'zh-Hans',{numeric:true})); return rows;},[matches,filters]); const setAndSave=(rows:MatchPrediction[])=>{setMatches(rows); saveMatches(rows)}; return <main className="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-cyan-950 p-4 text-slate-100 md:p-8"><div className="mx-auto max-w-[1800px] space-y-5"><DashboardCards matches={matches}/><ImportExportPanel matches={matches} onAdd={()=>setAndSave([...matches,emptyMatch(Math.max(0,...matches.map(m=>m.matchNo))+1)])} onSave={()=>saveMatches(matches)} onRecalc={()=>setAndSave(recalcAll())} onReset={()=>{if(confirm('确定重置为内置示例数据吗？')) setMatches(resetMatches())}} onImport={(text)=>{try{setAndSave(importMatches(text)); alert('导入成功')}catch(e){alert(e instanceof Error?e.message:'导入失败')}}}/><Filters filters={filters} setFilters={setFilters}/><MatchTable matches={visible} onOpen={setEditing} onDelete={id=>setAndSave(matches.filter(m=>m.id!==id))} onUpdate={(id,patch)=>setMatches(matches.map(m=>m.id===id?recalculateMatch({...m,...patch}):m))}/><InfoPanel/><MatchEditor match={editing} onChange={setEditing} onClose={()=>setEditing(null)} onSave={()=>{if(editing){setAndSave(matches.map(m=>m.id===editing.id?recalculateMatch(editing):m)); setEditing(null)}}}/></div></main> }
